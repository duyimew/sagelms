apiVersion: apps/v1
kind: Deployment
metadata:
  name: assessment-service
  labels:
    app.kubernetes.io/name: assessment-service
    app.kubernetes.io/part-of: sagelms
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: assessment-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: assessment-service
        app.kubernetes.io/part-of: sagelms
    spec:
      serviceAccountName: assessment-service
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: assessment-service
          image: harbor.sagelms.dev/sagelms-app/assessment-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8085
          envFrom:
            - configMapRef:
                name: assessment-service-config
            - secretRef:
                name: db-app-secret
            - secretRef:
                name: app-shared-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop:
                - ALL
