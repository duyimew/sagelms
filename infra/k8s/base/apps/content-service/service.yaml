apiVersion: v1
kind: Service
metadata:
  name: content-service
  labels:
    app.kubernetes.io/name: content-service
    app.kubernetes.io/part-of: sagelms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: content-service
  ports:
    - name: http
      port: 8083
      targetPort: http
