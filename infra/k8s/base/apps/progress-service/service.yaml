apiVersion: v1
kind: Service
metadata:
  name: progress-service
  labels:
    app.kubernetes.io/name: progress-service
    app.kubernetes.io/part-of: sagelms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: progress-service
  ports:
    - name: http
      port: 8084
      targetPort: http
